# Pretty Docs

The library exposes a small public surface:

- `PrettyOptions` for formatter configuration
- `Spec`, `SpecLeaf`, `SpecContainer`, and `SpecItem` variants for structural description
- `pdoc`, `pformat`, and `pprint` as the top-level entrypoints

Everything else is private implementation detail.
